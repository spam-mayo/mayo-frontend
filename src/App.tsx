import A from '@/components/A';

const App = () => {
  const onClick = () => {
    //
  };
  return (
    <div>
      Spam Mayo Frontend
      <A />
      <button onClick={onClick}>BTN</button>
    </div>
  );
};

export default App;
